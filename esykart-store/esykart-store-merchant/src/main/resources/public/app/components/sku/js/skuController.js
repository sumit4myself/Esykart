angular.module('altairApp')
.controller(
		'AddSkuController',
		[ '$scope', '$rootScope', 'utils', 'SkuService',
		function($scope, $rootScope, utils, SkuService) {

			
			
			
			
		} ])

.controller(
		'ManageSkuController',
		[ '$scope', '$rootScope', 'utils', 'SkuService',
		function($scope, $rootScope, utils, SkuService) {

			
			
			
			
		} ])